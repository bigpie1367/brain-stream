# Documentation Sync v2.0.0 — Implementation Plan

- **작성일**: 2026-03-21
- **Spec**: `docs/superpowers/specs/2026-03-21-docs-sync-design.md`
- **범위**: 문서 전용 (코드 변경 없음)

---

## Steps

### Step 1: .gitignore 업데이트
- `.worktrees/` 항목 추가

### Step 2: docs/architecture.md 업데이트
- 버전 2.0.0으로 갱신
- 컴포넌트 역할 테이블에 `src/jobs.py`, `src/pipeline/musicbrainz.py`, `src/utils/fs.py` 추가

### Step 3: docs/api-spec.md 업데이트
- 버전 2.0.0으로 갱신
- `/health` 엔드포인트 섹션 추가
- `GET /api/downloads` 페이지네이션 스펙으로 교체 (limit/offset/search, items+total 응답)
- `POST /api/download` 중복 감지 동작 문서화

### Step 4: docs/data-model.md 업데이트
- 버전 2.0.0으로 갱신
- state.py 주요 함수 참조 테이블 추가 (11개 함수)

### Step 5: docs/operations.md 업데이트
- 버전 2.0.0으로 갱신
- 섹션 3.5 헬스체크 추가
- `.dockerignore` 설명 추가

### Step 6: docs/requirements.md 업데이트
- 버전 2.0.0으로 갱신
- FR-08 페이지네이션/검색/무한스크롤로 갱신
- FR-09 중복 다운로드 방지 추가

### Step 7: CLAUDE.md 업데이트
- Key Files 테이블에 3개 신규 모듈 추가
